* Add logrotate
* Allow connections only from install hosts external ip to proxy
* Allow dns queries only from local network/install host if dns is facing the internet
* Add ssh keys to the box automatically
* Don't run apt-get install python if it is already installed
